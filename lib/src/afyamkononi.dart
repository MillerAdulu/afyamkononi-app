import 'package:flutter/material.dart';

import 'package:afyamkononi/src/screens/decision.dart';

class AfyaMkononi extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Auth Flow',
      home: DecisionPage(),
    );
  }
}
